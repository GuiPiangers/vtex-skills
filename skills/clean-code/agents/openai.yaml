interface:
  display_name: "Clean Code"
  short_description: "Pragmatic code quality guidelines"
  default_prompt: "Use $clean-code to keep edits minimal, readable, and production-safe. Prefer the simplest working solution; avoid over-engineering and unnecessary abstractions."
